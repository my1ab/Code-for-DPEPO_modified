# DPEPO 奖励机制解析 — 论文公式与代码实现对照

## 概述

本文档将 DPEPO 论文中提出的层次化奖励体系与 `rollout_loop_parallel_webshop.py` 及下游 `ray_trainer.py` / `core_algos.py` 中的代码实现逐一对应。

---

## 一、Parallel Trajectory-level Success Reward（并行轨迹级成功奖励）

### 论文公式

$$R_{\text{traj}}(\tau) = \begin{cases} 1 & \text{if } S_T \cap G \neq \varnothing \\ 0 & \text{otherwise} \end{cases}$$

**含义**：K 个并行环境中，任一环境达到目标状态即视为成功。

### 代码对应

| 步骤 | 代码位置 | 关键逻辑 |
|------|---------|---------|
| ① 环境返回每个并行环境的奖励列表 | `rollout_loop_parallel_webshop.py` / `vanilla_multi_turn_loop` L574 | `np_rewards = np.array(single_dict_grouped_output['rewards'])` |
| ② 取最大值（任一环境成功即成功） | `rollout_loop_parallel_webshop.py` / `vanilla_multi_turn_loop` L575-577 | `rewards = np.array([np.max(lst) if len(lst) > 0 else 0 for lst in np_rewards])` |
| ③ 逐步累加到轨迹级奖励 | `rollout_loop_parallel_webshop.py` / `vanilla_multi_turn_loop` L585 | `episode_rewards[active_masks] += torch_to_numpy(rewards)[active_masks]` |
| ④ 判断是否成功（奖励>0） | `rollout_loop_parallel_webshop.py` / `vanilla_multi_turn_loop` L633-639 | `for reward in current_rewards: if reward > 0: success_flags[bs] = 1` |

### 对应关系图

```
论文:  R_traj(τ) = 1  if ANY env succeeds
                  0  otherwise
                    │
                    ▼
代码:  max(np_rewards[batch]) > 0  →  success = 1
       episode_rewards[bs] = Σ max(env_rewards[step])  (逐步累加)
```

---

## 二、Diverse Action Reward（多样性动作奖励）

### 论文公式

$$R_{\text{action}}(A_t) = \frac{1}{|E'_t|} \sum_{E_i \in E'_t} \alpha C_{\text{depth}}(E_i, a_t) + \omega C_{\text{width}}(A_t)$$

| 符号 | 含义 |
|------|------|
| $C_{\text{depth}}(E_i, a_t)$ | 动作 $a_t$ 在环境 $E_i$ 历史中的重复次数 |
| $C_{\text{width}}(A_t)$ | 并行动作集中重复动作数 |
| $\alpha, \omega$ | 深度/宽度折扣因子 |

### 代码对应

#### 2.1 深度重复计数 $C_{\text{depth}}$

**论文**：$\alpha C_{\text{depth}}$
**代码**：$\alpha^{\text{COUNT\_repeat\_penalty}}$

**所在文件**：`agent_system/multi_turn_rollout/rollout_loop_parallel_webshop.py`，`TrajectoryCollectorParallelWebShop` 类

| 代码位置 | 函数 | 逻辑 |
|---------|------|------|
| L460-463 | `calculate_depth_repeat` | 从后往前统计动作在历史中重复次数 |
| L389-391 | `calculate_penalties` | 调用 `calculate_depth_repeat` 得到 `COUNT_repeat_penalty` |
| L408 | `calculate_penalties` | `W_depth_repeat = depth_alpha ** COUNT_repeat_penalty` |

```python
def calculate_depth_repeat(self, history_actions, action):
    """论文公式中的 C_depth(E_i, a_t) — 文件: rollout_loop_parallel_webshop.py L460"""
    repeat_count = 0
    for history_action in reversed(history_actions):
        if action == history_action:
            repeat_count += 1
    return repeat_count
```

#### 2.2 宽度重复计数 $C_{\text{width}}$

**论文**：$\omega C_{\text{width}}$
**代码**：$\omega^{\text{COUNT\_width\_repeat}}$

**所在文件**：`agent_system/multi_turn_rollout/rollout_loop_parallel_webshop.py`，`TrajectoryCollectorParallelWebShop.calculate_penalties`

| 代码位置 | 逻辑 |
|---------|------|
| L418-419 | `actions_wo_look = [a for a in action_dict.values() if a != 'look']` |
| L420 | `COUNT_width_repeat = len(actions_wo_look) - len(set(actions_wo_look))`（去重后减少的长度 = 重复数） |
| L422 | `W_width_repeat = width_omega ** COUNT_width_repeat` |

#### 2.3 完整对应

```
论文:  R_action = (1/|E'|) Σ α·C_depth  +  ω·C_width
                    │                           │
                    ▼                           ▼
代码:  pooling_w_action = mean(W_depth_repeat)   W_width_repeat
       W_depth_repeat = depth_alpha ** C_depth   W_width_repeat = width_omega ** C_width
       
差异:  代码使用指数折扣(α^C) 替代 论文的加法折扣(α·C)
```

---

## 三、Diverse State Transition Reward（多样性状态转移奖励）

### 论文公式

$$R_{\text{transition}}(A_t) = \frac{1}{|E'_t|} \sum_{E_i \in E'_t} \gamma M_{\text{depth}}(E_i, p_t) + \frac{1}{|E'_t|} \sum_{E_i \in E'_t} \beta M_{\text{width}}(E_i, p_t)$$

其中转移对 $p_t = s_t \to a_t$，即 (前一动作, 当前动作) 组成的转移对。

| 符号 | 含义 |
|------|------|
| $M_{\text{depth}}(E_i, p_t)$ | 转移对 $p_t$ 在环境 $E_i$ 内的历史重复次数 |
| $M_{\text{width}}(E_i, p_t)$ | 转移对 $p_t$ 在其他并行环境中被观测到的次数 |
| $\gamma, \beta$ | 深度/宽度转移折扣因子 |

### 代码对应

#### 3.1 转移对构造

**所在文件**：`agent_system/multi_turn_rollout/rollout_loop_parallel_webshop.py`，`TrajectoryCollectorParallelWebShop` 类

```python
def get_state_action_pair(self, action_history):
    """构造 (prev_action, curr_action) 转移对列表 — rollout_loop_parallel_webshop.py L447"""
    return [(a, b) for a, b in zip(action_history, action_history[1:])]
```

#### 3.2 转移重复计数

```python
def calculate_transition_repeat(self, history_actions, last_state_action):
    """计算转移对在历史中的重复次数 — rollout_loop_parallel_webshop.py L451"""
    full_action_list = history_actions
    state_action_pair_a = self.get_state_action_pair(full_action_list)
    if len(state_action_pair_a) == 0:
        return 0
    repeat_count = state_action_pair_a.count(last_state_action)
    return repeat_count
```

#### 3.3 深度转移 $M_{\text{depth}}$（同环境内）

**所在文件**：`agent_system/multi_turn_rollout/rollout_loop_parallel_webshop.py`，`TrajectoryCollectorParallelWebShop.calculate_penalties`

| 代码位置 | 逻辑 |
|---------|------|
| L394-399 | `last_state_action = (env_history_actions[-1], env_action)` |
| L394-399 | `COUNT_depth_transition_penalty = self.calculate_transition_repeat(history_actions, last_state_action)` |
| L411 | `W_depth_t_repeat = depth_t_gamma ** COUNT_depth_transition_penalty` |

#### 3.4 宽度转移 $M_{\text{width}}$（跨环境间）

**所在文件**：`agent_system/multi_turn_rollout/rollout_loop_parallel_webshop.py`，`TrajectoryCollectorParallelWebShop.calculate_penalties`

| 代码位置 | 逻辑 |
|---------|------|
| L401-406 | 遍历其他并行环境，将 (该环境的当前动作) 拼入历史后检查转移对重复 |
| L407 | `COUNT_width_transition_repeat = sum(LIST_width_repeat_count)` |
| L412 | `W_width_t_repeat = width_t_beta ** COUNT_width_transition_repeat` |

#### 3.5 完整对应

```
论文:  R_transition = (1/|E'|) Σ γ·M_depth  +  (1/|E'|) Σ β·M_width
                               │                            │
                               ▼                            ▼
代码:  W_depth_t_repeat      W_width_t_repeat
       = depth_t_gamma ^ M_depth   = width_t_beta ^ M_width
       
差异:  代码使用指数折扣(γ^M) 替代 论文的加法折扣(γ·M)
```

---

## 四、Step-Level Composite Reward（步级复合奖励）

### 论文公式

$$R_{\text{step}}(A_t) = \frac{R_{\text{action}}(A_t) + R_{\text{transition}}(A_t)}{2}$$

**所在文件**：`agent_system/multi_turn_rollout/rollout_loop_parallel_webshop.py`，`TrajectoryCollectorParallelWebShop.calculate_penalties`（L375-427）

代码将四个组件（depth_repeat, depth_transition, width_transition, width_repeat）聚合为单一权重 `W`，而非分离的 `R_action` 和 `R_transition`：

```python
# 单环境的 3 个组件取平均（深度重复 + 深度转移 + 宽度转移）
W_list = [W_depth_repeat, W_depth_t_repeat, W_width_t_repeat]
pooling_kind_weight = sum(W_list) / len(W_list)   # 每个环境的内部平均

# 跨所有环境平均
pooling_w_action = mean(action_penalty_per_env)     # 跨环境聚合

# 最终：宽度重复 + 环境聚合的步级惩罚，两者再平均
W = (W_width_repeat + pooling_w_action) / 2
```

### 聚合公式对比

```
论文:  R_step = (R_action + R_transition) / 2
                      │
                      ▼
代码:  W = ( W_width_repeat                                ← 对应 ω·C_width(At)
           + mean(mean(W_depth_repeat,                     ← 对应 Σ α·C_depth(Ei)
                       W_depth_t_repeat,                   ← 对应 Σ γ·M_depth(Ei)
                       W_width_t_repeat)))                 ← 对应 Σ β·M_width(Ei)
           / 2
```

### 步级奖励在完整流程中的位置

```
rollout_loop_parallel_webshop.py           ray_trainer.py / core_algos.py
┌─────────────────────────────┐            ┌─────────────────────────────┐
│ 每步调用 calculate_penalties │            │ compute_grpo_outcome_advantage
│ → 计算步级多样性惩罚 W       │            │                              │
│ → 存入 sample['penalty_W']  │  ───────► │ add_step_penalty=True 时:     │
│                             │            │   if Φ_traj > 0:             │
│ 每步累加环境奖励             │            │     Φ_step = W   (鼓励多样性) │
│ → episode_rewards           │            │   else:                      │
│                             │            │     Φ_step = 2-W (抑制过度探索)│
│ gather_rollout_data         │            │   Φ = Φ_step · Φ_traj        │
│ → episode_rewards 传递给     │            │                              │
│   PPO 作为 token_level_rewards│           │ GRPO loss = f(advantages)    │
└─────────────────────────────┘            └─────────────────────────────┘
```

---

## 五、优势函数（Advantage）计算

### 论文公式

**轨迹级优势**：
$$\Phi_{\text{traj}}(\tau_i) = \frac{R(\tau_i) - \text{mean}}{\text{std}}$$

**步级优势条件组合**：
$$\Phi_{\text{step}}(A_{i,t}) = \begin{cases} R_{\text{step}}(A_{i,t}) & \text{if } \Phi_{\text{traj}}(\tau_i) > 0 \\ 2 - R_{\text{step}}(A_{i,t}) & \text{otherwise} \end{cases}$$

**最终优势**：
$$\Phi(A_{i,t}) = \Phi_{\text{step}}(A_{i,t}) \cdot \Phi_{\text{traj}}(\tau_i)$$

**所在文件**：`verl/trainer/ppo/core_algos.py`，模块级函数 `compute_grpo_outcome_advantage`（L113-185）

**上游调用**：`verl/trainer/ppo/ray_trainer.py` / `ray_trainer_parallel.py` 中的 `compute_advantage` 函数（L283-317）将 `penalty_W` 和 `add_step_penalty` 传入 `core_algos.py`

```python
def compute_grpo_outcome_advantage(token_level_rewards, response_mask, index, traj_index, ...):
    """文件: verl/trainer/ppo/core_algos.py L113"""
    scores = token_level_rewards.sum(dim=-1)

    # ---- Φ_traj: 按 uid 组内标准化（对应公式 7） ----
    id2score[index[i]].append(scores[i])
    # ...
    scores[i] = (scores[i] - id2mean[index[i]]) / (id2std[index[i]] + epsilon)

    # ---- Φ_step: 条件组合（对应公式 8） ----
    if add_step_penalty:
        if torch.all(scores[i] < 0):        # Φ_traj ≤ 0 → 负优势轨迹
            W = 2 - step_penalty_list[i]    # Φ_step = 2 - R_step
        else:                                # Φ_traj > 0 → 正优势轨迹
            W = step_penalty_list[i]         # Φ_step = R_step
        scores[i] = scores[i] * W            # Φ = Φ_step · Φ_traj（公式 9）

    return scores, scores
```

### 关键配置

| 配置项 | 位置 | 作用 |
|-------|------|------|
| `reward_model.parallel_reward=true` | shell 脚本 / yaml 配置文件 | 启用步级多样性惩罚，传递给 `add_step_penalty` |
| `reward_model.process_reward=false` | shell 脚本 / yaml 配置文件 | 是否启用 LCS 过程奖励（替代失败轨迹的奖励） |
| `algorithm.adv_estimator=grpo` | shell 脚本 / yaml 配置文件 | 使用 GRPO 优势估计器 |

### 配置参数映射

| 论文参数 | 代码配置 | 默认值 |
|---------|---------|--------|
| $\alpha$（深度动作折扣） | `reward_model.depth_alpha` | 0.8 |
| $\omega$（宽度动作折扣） | `reward_model.width_omega` | 0.95 |
| $\gamma$（深度转移折扣） | `reward_model.depth_t_gamma` | 0.95 |
| $\beta$（宽度转移折扣） | `reward_model.width_t_beta` | 0.95 |

---

## 六、附录：奖励计算函数一览表

| 论文组件 | 论文公式 | 代码函数 | 所在文件 | 类 | 代码行 |
|---------|---------|---------|---------|----------|--------|
| 轨迹级成功奖励 | $R_{\text{traj}} = \mathbb{1}[S_T \cap G \neq \varnothing]$ | `vanilla_multi_turn_loop`（环境奖励取max） | 三者共有¹ | `TrajectoryCollectorParallel*` | L585-591² |
| 深度动作重复 $C_{\text{depth}}$ | $\alpha \cdot C_{\text{depth}}$ | `calculate_depth_repeat` | 三者共有¹ | `TrajectoryCollectorParallel*` | ~L790³ |
| 宽度动作重复 $C_{\text{width}}$ | $\omega \cdot C_{\text{width}}$ | `calculate_width_repeat_rate` | 三者共有¹ | `TrajectoryCollectorParallel*` | ~L800³ |
| 深度转移重复 $M_{\text{depth}}$ | $\gamma \cdot M_{\text{depth}}$ | `calculate_transition_repeat`（同环境） | 三者共有¹ | `TrajectoryCollectorParallel*` | ~L775³ |
| 宽度转移重复 $M_{\text{width}}$ | $\beta \cdot M_{\text{width}}$ | `calculate_transition_repeat`（跨环境） | 三者共有¹ | `TrajectoryCollectorParallel*` | ~L770³ |
| 步级惩罚权重 $W$ | $R_{\text{step}} = (R_{\text{action}}+R_{\text{transition}})/2$ | `calculate_penalties` | 三者共有¹ | `TrajectoryCollectorParallel*` | ~L700³ |
| 轨迹级优势 $\Phi_{\text{traj}}$ | $\text{group\_normalize}(R)$ | `compute_grpo_outcome_advantage` | `verl/trainer/ppo/core_algos.py` | 模块级函数 | L113-185 |
| 步级优势 $\Phi_{\text{step}}$ | $\Phi_{\text{step}} = \begin{cases} W & \Phi_{\text{traj}}>0 \\ 2-W & \text{otherwise}\end{cases}$ | `compute_grpo_outcome_advantage`（`add_step_penalty`） | `verl/trainer/ppo/core_algos.py` | 模块级函数 | L175-180 |
| 最终优势 $\Phi$ | $\Phi = \Phi_{\text{step}} \cdot \Phi_{\text{traj}}$ | `scores[i] = scores[i] * W` | `verl/trainer/ppo/core_algos.py` | 模块级函数 | L179 |
| LCS 过程奖励（论文外） | — | `calculate_process_reward` / `count_longest_ordered_subsequence` | 三者共有¹ | `TrajectoryCollectorParallel*` | ~L650³ |

> **注¹**：三者指 `rollout_loop_parallel.py`(ALFWorld)、`rollout_loop_parallel_webshop.py`(WebShop)、`rollout_loop_parallel_sciworld.py`(ScienceWorld)，核心函数实现一致。
> **注²**：三个文件的 `vanilla_multi_turn_loop` / `multi_turn_loop` 轨迹收集循环中，环境奖励取 max 并累加的代码完全一致。
> **注³**：各文件行号略有偏移（因文件长度不同），具体行号见第 §8.3 节。WebShop 版本在 `calculate_penalties` 中有 `action_penalty_per_env` 空值保护，ScienceWorld 版本额外集成了 `invalid_penalty`。

---

## 七、论文与代码的主要差异总结

| 差异项 | 论文公式 | 代码实现 | 影响 |
|-------|---------|---------|------|
| 折扣方式 | 加法折扣 $\alpha \cdot C$ | 指数折扣 $\alpha^C$ | 指数形式对高重复惩罚更严厉，$C=3$ 时：$0.5\cdot3=1.5$ vs $0.5^3=0.125$ |
| 步级奖励结构 | 分离的 $R_{\text{action}}$ 和 $R_{\text{transition}}$ | 聚合为单一权重 `W` | 功能等价，但降低了结构清晰度 |
| `look` 动作处理 | 未提及 | 宽度重复计数时排除 `look` | 工程优化，避免 trivial 动作干扰多样性度量 |
| 过程奖励 (LCS) | 未提及 | 可选的 `process_reward` | 经验性的性能增强，任务失败时提供软信号 |

---

## 八、三份 rollout 文件的奖励机制对比

项目中三份平行环境 rollout 文件实现了相同的 DPEPO 核心奖励机制，但在细节上有差异。

### 8.1 文件定位

| 文件 | 对应环境 | 类名 |
|------|---------|------|
| `agent_system/multi_turn_rollout/rollout_loop_parallel.py` | ALFWorld | `TrajectoryCollectorParallel` |
| `agent_system/multi_turn_rollout/rollout_loop_parallel_webshop.py` | WebShop | `TrajectoryCollectorParallelWebShop` |
| `agent_system/multi_turn_rollout/rollout_loop_parallel_sciworld.py` | ScienceWorld | `TrajectoryCollectorParallelSciWorld` |

### 8.2 共同点（✅ 完全一致的机制）

| 机制 | 说明 |
|------|------|
| 轨迹级成功奖励 | 三者完全一致：`np.max(np_rewards)` → 累加到 `episode_rewards` |
| `calculate_depth_repeat` | 完全相同：从后往前统计动作重复次数 |
| `calculate_width_repeat_rate` | 完全相同：去重后计算宽度重复数 |
| `get_state_action_pair` | 完全相同：构造 `(prev_act, curr_act)` 转移对 |
| `calculate_transition_repeat` | 完全相同：统计转移对重复次数 |
| `count_longest_ordered_subsequence` | 完全相同：LCS 过程奖励算法 |
| `calculate_process_reward` | 完全相同：取 K 个并行环境中 LCS 匹配度最高的一个 |
| 配置参数 | 均使用 `depth_alpha`, `width_omega`, `depth_t_gamma`, `width_t_beta` |

### 8.3 `calculate_penalties` 函数差异

这是步级多样性惩罚的核心函数，三者的差异如下：

```python
# ALFWorld: rollout_loop_parallel.py L694
def calculate_penalties(self, history_actions, action_dict):
    # ... 计算 C_depth, M_depth, M_width, C_width ...
    W = (W_width_repeat + pooling_w_action) / 2
    #     ↑ 对应 ω^C_width   ↑ 对应 mean(α^C_depth, γ^M_depth, β^M_width)

# WebShop: rollout_loop_parallel_webshop.py L572
def calculate_penalties(self, history_actions, action_dict):
    # ... 同上，完全相同的核心逻辑 ...
    # 额外处理: 对空action_dict返回 no_action_penalty
    pooling_w_action = sum(action_penalty_per_env) / len(action_penalty_per_env) if action_penalty_per_env else 0.0
    W = (W_width_repeat + pooling_w_action) / 2

# ScienceWorld: rollout_loop_parallel_sciworld.py L709
def calculate_penalties(self, history_actions, action_dict, observations):
    # ... 同上核心逻辑 ...
    # 额外: 通过 observations 统计无效动作次数
    for sub_obs in observations:
        if 'No known action matches that input.' in sub_obs:
            COUNT_invalid_actions += 1
    W_invalid_actions = invalid_penalty ** COUNT_invalid_actions
    W = (W_width_repeat + pooling_w_action + W_invalid_actions) / 3
```

### 8.4 关键差异汇总表

| 对比项 | ALFWorld | WebShop | ScienceWorld |
|-------|---------|---------|-------------|
| `calculate_penalties` 参数 | `(history_actions, action_dict)` | `(history_actions, action_dict)` | `(history_actions, action_dict, observations)` |
| 无效动作惩罚 | ❌ 无 | ❌ 无 | ✅ 内置在 `calculate_penalties` 中 |
| 最终聚合公式 | $(W_{\text{width}} + \text{pooling}) / 2$ | $(W_{\text{width}} + \text{pooling}) / 2$ | $(W_{\text{width}} + \text{pooling} + W_{\text{invalid}}) / 3$ |
| 空 action_dict 保护 | ❌ `pooling_w_action` 可能报除零 | ✅ `if action_penalty_per_env else 0.0` | ❌ `pooling_w_action` 可能报除零 |
| expert_actions 缺省处理 | ❌ 无保护 | ✅ `if 'expert_actions' in trajectory[0]` | ❌ 无保护 |

### 8.5 ScienceWorld 特殊逻辑：`calculate_invalid_actions_penalties`

ScienceWorld 在并行惩罚计算中存在一个值得注意的双重写入：

```python
# 第一段（受 parallel_reward 配置开关控制）：
if self.config.reward_model.parallel_reward:
    W = self.calculate_penalties(...)    # 计算完整 DPEPO 多样性惩罚 + 无效动作惩罚
    sample['penalty_W'] = W              # 写入 penalty_W

# 第二段（始终执行，不受 parallel_reward 控制）：
for sample, cur_history_actions in zip(...):
    W = self.calculate_invalid_actions_penalties(...)  # 只计算无效动作惩罚
    sample['penalty_W'] = W              # ⚠️ 覆盖写入，擦除了上一段的结果
```

这意味着 **ScienceWorld 实际生效的 `penalty_W` 始终只是无效动作惩罚**，之前的 DPEPO 多样性惩罚（深度重复、宽度重复、转移重复）被第二段覆盖丢弃了。这可能是代码演进中的残留问题。

### 8.6 建议：统一在 `奖励解析.md` 中引用哪个文件？

- **主要参考文件**：`rollout_loop_parallel_webshop.py` — 代码最完整、保护逻辑最完善
- **核心奖励逻辑**：三者 `calculate_penalties` 的 DPEPO 核心部分（不含 invalid_penalty）完全一致
- **下游 advantage 计算**：三者共享同一套 `ray_trainer.py` → `core_algos.py` 计算链
