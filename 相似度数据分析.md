# WebShop 嵌入相似度实测数据分析

> 日志: `test_bert_webshop_20260716_015924.log`
> 模型: BAAI/bge-base-en-v1.5, CLS pooling + Normalize, 768d, float16
> 当前阈值: 0.85

---

## 一、关键数据提取

### 1.1 阈值判定错误（4/26 = 15.4% 错误率）

| # | 动作A | 动作B | 相似度 | 阈值判定 | 期望 | 错误类型 |
|---|---|---|---|---|---|---|
| 1 | `search[red shoes]` | `search[crimson footwear]` | **0.8358** | 不重复 | 重复 | **漏检**（同义词） |
| 2 | `search[laptop bag]` | `search[computer case]` | **0.7966** | 不重复 | 重复 | **漏检**（同义词） |
| 3 | `search[red shoes]` | `search[red shirt]` | **0.8556** | 重复 | 不重复 | **误检**（同色不同物） |
| 4 | `search[red shoes]` | `search[red socks]` | **0.9130** | 重复 | 不重复 | **误检**（同类不同物） |

### 1.2 相似度分布（55 对，排除自身）

```
[0.9, 1.0):   3  ███
[0.8, 0.9):   8  ████████
[0.7, 0.8):   8  ████████
[0.6, 0.7):  20  ████████████████████
[0.5, 0.6):  15  ███████████████
[0.0, 0.5):   0
```

**问题**：所有相似度都集中在 [0.5, 1.0]，没有低于 0.5 的。这说明 bge-base 对短文本的区分度不足，`search[`/`click[` 前缀贡献了结构性基线相似度。

### 1.3 阈值分离分析

```
应判为重复的最低相似度:   0.7966  (search[laptop bag] vs search[computer case])
不应判为重复的最高相似度: 0.9130  (search[red shoes] vs search[red socks])
分离间隙: 0.7966 - 0.9130 = -0.1164  （负值！存在严重重叠）
```

**结论**：不存在任何阈值能完美区分"应重复"和"不应重复"，两类在 [0.7966, 0.9130] 区间内完全重叠。

### 1.4 关键矛盾对

| 矛盾 | 动作A | 动作B | 相似度 | 语义关系 |
|---|---|---|---|---|
| **漏检** | `search[red shoes]` | `search[crimson footwear]` | 0.836 | 同义词，应判重复 |
| **误检** | `search[red shoes]` | `search[red socks]` | 0.913 | 不同物品，不应判重复 |

`red socks`（0.913）比 `crimson footwear`（0.836）相似度更高，但语义上前者更不同。模型被 `search[red` 前缀和单词 `red` 主导。

---

## 二、问题根因

### 2.1 前缀污染

`search[` 前缀在所有 search 动作中完全相同，导致：
- `search[red shoes]` vs `search[blue shirt]` = 0.680（不应有任何相似度）
- `search[red shoes]` vs `search[red socks]` = 0.913（前缀+颜色词主导）

### 2.2 短文本信息量不足

WebShop 动作平均只有 3-5 个 token，bge-base 的 CLS pooling 在短文本上区分度降低。相比之下，通用英文句子（测试 1-3）有 8-15 个 token，区分度更好。

---

## 三、优化建议

### 3.1 动作预处理（零成本，立即可做）

**去掉 `search[` / `click[` 前缀再嵌入**，消除结构性基线相似度：

```python
def preprocess_action(action):
    """去掉动作前缀，只保留语义内容。"""
    if action.startswith("search[") and action.endswith("]"):
        return action[7:-1]        # "red shoes"
    if action.startswith("click[") and action.endswith("]"):
        return action[6:-1]        # "Buy Now"
    return action
```

预期效果：
- `search[red shoes]` vs `search[red socks]` -> 嵌入 `"red shoes"` vs `"red socks"`（去掉 search 前缀）
- `search[red shoes]` vs `click[Buy Now]` -> 嵌入 `"red shoes"` vs `"Buy Now"`（跨类型相似度大幅降低）

### 3.2 阈值调整

**当前 0.85 的问题**：
- `red shirt`（0.856）刚好超过 0.85 被误判为重复
- `crimson footwear`（0.836）刚好低于 0.85 被漏检

**如果加预处理后**：相似度分布会重新拉开，阈值可能需要在预处理后的数据上重新评估。建议预处理后重新跑测试，再定阈值。

**不预处理的情况**：不存在好的阈值（重叠区间 0.12），0.85 是"两个错误"的折中点。降到 0.83 会多检 `crimson footwear` 但也会多误检 `red shirt`。

### 3.3 模型选型

| 模型 | 参数量 | 优势 | 风险 |
|---|---|---|---|
| bge-base-en-v1.5（当前） | 110M | 已验证 | 短文本区分度不足 |
| **bge-large-en-v1.5** | 335M | 更强语义理解，可能改善同义词 | 显存 1.3GB，推理略慢 |
| **intfloat/e5-large-v2** | 335M | 对短文本和商品描述更友好 | 需要加 `"query: "`/`"passage: "` 前缀 |
| **BAAI/bge-m3** | 568M | 多语言 + 多粒度 | 显存 2.2GB，可能过重 |

**推荐**：先试 `bge-large-en-v1.5`，参数量增加 3 倍但仍可接受（1.3GB 显存），可能在同义词识别上有显著提升。

### 3.4 进一步实验建议

1. **预处理 + 原模型**（最快验证）：在 `EmbeddingManager` 中加 `preprocess_action()`，重新跑测试脚本
2. **预处理 + 大模型**：如果预处理后仍有重叠，换 `bge-large-en-v1.5` 再测
3. **扩大测试集**：当前只有 33 对，增加到 50-100 对，覆盖更多商品类目（电子产品、家居、服装等）

---

## 四、对惩罚机制的实际影响评估

即使有 4 个错误，对最终 $W$ 的影响有限：

- 错误 3（`red shirt` 误检）：多计 1 次重复 -> $C$ 多 0.856 -> $W_{\text{depth}} = 0.8^{0.856} = 0.879$ vs 正确的 $0.8^0 = 1.0$，差异 12%
- 错误 4（`red socks` 误检）：类似，$W$ 偏低约 15%
- 但经过组内平均（5 个环境）和 $W = (W_{\text{width}} + \text{pooling})/2$，单步错误被进一步稀释

**结论**：当前模型+阈值可用但不理想，建议优先实施动作预处理（3.1），这是零成本改动中最可能有效的。
